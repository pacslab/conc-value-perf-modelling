version: "3"

services:
  worker:
    build: .
    command: sleep infinity
    environment: 
      - "HTTP_PATH=$HTTP_PATH"
      - "EXP_NAME=$EXP_NAME"
      - "LOGGER_PATH=$LOGGER_PATH"
      - "PY_TZ=US/Eastern"
    volumes:
      - $HOME_DIR/src:/opt/src
