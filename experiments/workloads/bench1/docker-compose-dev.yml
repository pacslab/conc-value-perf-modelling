version: "3"

services:
  worker:
    build: .
    ports:
      - "8080:8080"
    command: sleep infinity
    volumes:
      # - ./src:/opt/src
      - $HOME_DIR/src:/opt/src
    # resource limits
    cpu_percent: 25
    cpu_count: 1
    mem_limit: 256M
